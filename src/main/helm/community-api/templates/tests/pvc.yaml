apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: community-api-pvc
spec:
  storageClassName: nfs-storageclass
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
